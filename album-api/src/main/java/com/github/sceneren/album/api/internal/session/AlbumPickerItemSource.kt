package com.github.sceneren.album.api.internal.session

/** Origin retained while converting picker session items to public media models. */
internal enum class AlbumPickerItemSource {
    MEDIA_STORE,
    PHOTO_PICKER,
    CAMERA,
}
