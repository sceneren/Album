package com.github.sceneren.album.ui.view

/** Pixel metrics shared by all grid cell adapters and item decoration. */
internal data class GridMetrics(
    val spanCount: Int,
    val spacingPx: Int,
)
